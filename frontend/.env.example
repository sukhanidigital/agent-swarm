# Copy to .env and set for a build that should default to a specific backend (a mobile build, or a
# static deploy of the web app pointed at a real server) instead of guessing from window.location.
# Optional even then — the in-app Settings screen can set/override this per-device after install.
VITE_API_BASE_URL=https://your-domain.example.com
