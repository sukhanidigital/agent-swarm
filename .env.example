# Copy to .env at the repo root. Used by docker-compose.yml (backend + frontend build) and by
# `python swarm.py` directly for local dev.

# --- required ---
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# --- auth (see api/auth.py) ---
# Unset = no auth, matches today's local-dev behavior. Set this before the backend is reachable from
# anywhere but localhost — it runs shell/dev-agent commands against whatever repo_path it's given.
API_KEY=

# --- login screen (see api/main.py's /login) ---
# The frontend's login screen asks for an email + password, then exchanges them for API_KEY server-
# side — the key itself is never typed in or shown. Unset = /login always succeeds regardless of what
# it's sent, matching API_KEY's own no-auth-by-default convention; set both before this is reachable
# from anywhere but localhost.
LOGIN_EMAIL=
LOGIN_PASSWORD=

# --- docker-compose only ---
# Host directory containing the repos you'll point the app at; mounted read-write into the backend
# container at /repos — matches PROJECTS_ROOT's default below, so leave both alone under Docker.
REPOS_DIR=./repos
# Where "Select project"/"Manage projects" (api/main.py's /projects) creates new project repos — the
# path the user never has to see or type, since they only ever pick a project by name. Defaults to
# /repos, matching the container mount above; only set this for local non-Docker dev (`python swarm.py`
# directly), where there's no such mount and this should point at a real directory on your machine
# instead, e.g. ./repos.
PROJECTS_ROOT=
# Where a browser/phone can actually reach the backend (its public URL, not the compose service
# name) — baked into the frontend's JS bundle at build time. Leave unset for local-network testing;
# the app's Settings screen can also set/override this per-device after the fact.
VITE_API_BASE_URL=

# --- optional: lessons-learned memory (see orchestrator/memory.py) ---
# Whole layer no-ops if unset — nothing else in the pipeline depends on it.
SUPABASE_URL=
SUPABASE_KEY=

# --- optional: job-finished email notification (see orchestrator/notify.py) ---
# No-ops if unset. NOTIFY_EMAIL_ADDRESS sends via Gmail SMTP using an app password (not your normal
# password) — generate one at https://myaccount.google.com/apppasswords (requires 2-Step Verification
# on the account). NOTIFY_EMAIL_TO defaults to NOTIFY_EMAIL_ADDRESS if unset.
NOTIFY_EMAIL_ADDRESS=
NOTIFY_EMAIL_APP_PASSWORD=
NOTIFY_EMAIL_TO=
